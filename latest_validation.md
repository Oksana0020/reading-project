# Latest Validation Notes

- The new local portal displays the Child, Teacher, and Parent demo cards with role-specific visual accents and no exposed passwords.
- Selecting the Child card opens a password prompt, and `child1` with the configured password successfully loads Amina Roe’s authorised child account.
- The child account shows Amina’s Reading Progress, a saved session, family connection code, and the teacher-assigned “The Lantern in the Garden” material.
- The explicit guided completion control opened the assigned-passage report successfully, including the child PDF and Quick Quiz actions.
- The child demo cohort now provisions an approved teacher assignment, a saved reading session, and quiz-ready exercises. Local child sign-in was verified with a password prompt; credentials are not contained in browser code.
- The completed child quiz displayed a 3/3 score and explanations. The visible Try Again control returned the child to a clean retry attempt, while quiz history remained available in the Reading Library.
- The teacher local demo account now lands on the Teacher Dashboard and exposes a saved-session audio control, feedback control, school-branding controls, and an authorised PDF running-record download. The teacher PDF trigger was successfully reached in the browser.
- The parent local demo account now lands on the Parent Dashboard and exposes its authorised branded parent-summary PDF download. The parent PDF trigger was successfully reached in the browser.
- The parent dashboard visibly displays “Download parent summary” with Amina’s role-scoped reading progress. This completes captured evidence for the separate child, teacher, and parent PDF report controls.
- The local role portal remains warm, responsive, and visually distinct by audience. Signing out returns cleanly to the protected local demo selection screen.
- The child demo card opens a masked password prompt only after role selection; no password value is displayed in the browser interface.
- The authenticated child demo library shows Amina Roe’s reading progress, a teacher-assigned Lantern passage, saved quiz history, and the child PDF-report shortcut. Opening the assigned passage shows the active Assisted Practice badge, three selectable mode controls, rounded canvas panel, and word-state controls.
- Guided Practice visibly shows its coach badge. After two tracked unsuccessful attempts, the app presented a model-audio prompt and the Reading Coach retry message; the next successful attempt advanced the word state and displayed the self-correction affirmation.
- Monthly Assessment hides correction/retry demonstrations, shows a quiet first-pass indicator, and uses the specific “Finish this monthly assessment” action. Its completed report showed first-pass story match and WCPM, quiet coach feedback, teacher review language, and no read-again correction action.
- The automated suite now includes deterministic live transcript state tests for Assisted, Guided, and Monthly modes. All eight test files and 25 tests pass.
- Assisted Practice was reopened on the assigned Lantern passage. The visible canvas presents the practice-with-corrections badge, yellow current-word state, and the accessible retry/self-correction controls before report completion.
- In live Assisted Practice, the child recorded an extra attempt on the active word, then self-corrected it. The canvas advanced to the next word and displayed the green self-correction affirmation, confirming the retry state transition.
- The live Assisted Practice retry state was inspected directly: the incorrect word used the `reader-word incorrect` class with rose background, rose text, and underline; the initial active word rendered with the yellow current-word treatment before the retry.
- The live self-corrected word was inspected directly: it used `reader-word retried_correct`, blue text, and a green retry-check marker. The next expected word moved into the current state, confirming the complete Assisted visual sequence.
- Guided Practice was reopened on the assigned passage with the coach-specific mode badge and the first active-word retry registered, ready for the two-attempt model prompt and report check.
- Guided Practice reached its second failed attempt and displayed the automatic model-audio toast and gentle retry message. The child then self-corrected the word, with the canvas advancing to the next current word.
- The completed Guided Practice report showed 97% story match, 64 WCPM, one self-correction, and the dedicated “Self-Corrections & Retries” section with the word’s two retries and corrected outcome.
- The active Assisted Practice word was inspected directly: it used `reader-word current` with an amber-yellow background, underline, and amber underline colour, completing the yellow/red/green word-state validation sequence.
- Monthly Assessment was selected in the live canvas and hid the visible retry controls, showing the quiet first-pass mode badge instead. The mode now uses its own completion wording and suppresses correction demonstrations.
- Teacher learner-plan persistence was rechecked with the local teacher and child demos. After saving Guided Practice at 112 WCPM, signing out and reopening the child account retained the plan and opened the assigned Lantern passage with the “Practice with a Reading Coach” mode badge.
- Local demo provisioning was corrected so its initial Assisted Practice plan is inserted only when absent and no longer overwrites a teacher’s subsequent saved plan.
- The authorised word-linked playback fixture renders a persistent audio element for inspectable playback. Instrumenting the “reads” cue captured a seek at 1.00 seconds, play at 1.00 seconds, and pause at 2.15 seconds for its 1.00–2.00 second word interval plus the intended 150 ms tail. The fixture is technical non-speech audio only; real saved recordings use Whisper segment-derived, approximate word timing.
- The authorised material-review page showed the full-width saved-material header, structured vocabulary cards, radio-style answer controls, and “Approve & Assign” action. Approval navigated directly to `/teacher/assignments/confirmation`, which displayed Ms Kelly’s Reading Class, class code `CLASS-READ`, and the Library shortcut.
- Authenticated mobile captures at a 375 px CSS viewport confirmed the material-review page reflows to a readable single-column document with stacked actions, vocabulary cards, radio-style answer rows, and an accessible approval CTA. Its measured document scroll width was 375 px, matching the viewport with no horizontal overflow.
- The authenticated mobile assignment-confirmation capture likewise measured a 375 px document scroll width at a 375 px viewport. The class details stacked clearly and the “View Assigned Activities in Library” action remained fully visible and touch-sized.
- Authenticated tablet captures at 768 px showed the material-review document with clearly readable exercise content, stacked vocabulary cards at the responsive breakpoint, full-width option rows, and a visible approval CTA. Its measured scroll width was 753 px within the 768 px viewport.
- The authenticated tablet assignment-confirmation route measured a 768 px document scroll width at a 768 px viewport and retained a balanced, readable confirmation card with class details and the Library action visible.
- A desktop teacher-dashboard capture visibly confirmed the monthly assessment chart, monthly labels, story-match and WCPM legend, learner-plan controls, saved-session playback access, and the materials workflow in one authorised role view.
- The desktop demo portal again displayed the three warm, role-coloured entry cards without a password field before role selection, preserving the single-focus password flow.
- The authorised parent dashboard was rechecked in the browser. It excluded teacher and administrator navigation, showed Minutes Read This Week plus celebratory Persistent Reader and Stories Shared badges, rendered the monthly assessment chart, and exposed three interactive home-practice checklist controls.
- Final validation completed with `pnpm check` and 11 passing test files / 30 passing tests. A fresh logout after removing the deprecated clear-cookie `maxAge` option produced no new Express deprecation entry; recent browser-console logs contained no runtime errors. Signed-out portal requests still log an expected missing-session notice, and the dev server emits a non-blocking Baseline browser-data freshness notice.
- The updated teacher demo dashboard loaded with the new Class manager, class-view selector, private roster/create-learner controls, and protected Export CSV action alongside the existing monthly trend chart. The all-classes view preserves distinct class labels in the learner overview.
- In the authorised teacher dashboard, selecting Ms Kelly’s specific class correctly revealed its join code, roster, class-specific trend title, and learner plan. The protected Export CSV action produced the visible download confirmation for the monthly assessment data.
- The downloaded class CSV was verified as `ms-kelly-s-reading-class-monthly-assessment-trends.csv`; it includes the expected Class, Month, Story Match %, WCPM, and Assessment Sessions columns with the same monthly values shown in the chart.
- The teacher created a second learner, Zara Khan, in Ms Kelly’s Reading Class. The roster, learner-plan list, and class overview all refreshed to show both learners; learners without sessions display a clear dash and are excluded from aggregate story-match and WCPM averages.
- In the linked parent account, the three home-practice steps persisted as they were checked. Completing the third step created one unread “Home practice complete” reminder in the Family reminder centre with Amina Roe’s supportive completion message.
- Marking the reminder read updated the Family reminder centre to “All caught up” while retaining the completion history. Unchecking and rechecking the final same-day step did not create a second reminder, confirming the duplicate-protection flow.
- Authenticated mobile captures at a 375 px viewport confirmed both the parent reminder-centre and the teacher class manager reflow into readable, touch-accessible single-column layouts. Each measured a 375 px document scroll width, with no horizontal overflow.
- The expanded suite now passes with TypeScript checking cleanly and 14 test files / 37 tests passing. It includes two database integration checks: authorised class creation/roster shaping and same-day parent reminder creation, read state, and de-duplication. Recent server and browser-console logs contain no new errors; the only displayed server warnings are historical pre-fix cookie deprecation entries.
- The authorised teacher selected Ms Kelly’s Reading Class and uploaded a CSV containing two valid learner rows plus one duplicate. Noah Brooks and Ivy Chen were added to the selected roster with their supplied book bands, while row 4 returned clear duplicate-name feedback without creating a second record.
- Applying the July 1–August 31, 2026 term range reduced the class trend chart to July and August only. The protected export generated `ms-kelly-s-reading-class-monthly-assessment-trends-2026-07-01-to-2026-08-31.csv`, containing the matching two monthly rows and assessment values.
- In the authorised parent account, completing the three home-practice steps created a current unread completion reminder. The dashboard displayed a header notification badge, a “1 new” reminder-centre status, and the “Mark all read” action; using it removed the unread badge and returned the centre to “All caught up” while preserving reminder history.
- Authenticated parent captures at 375 px and 768 px confirmed that the enhanced reminder centre, including the unread-state layout and completed practice controls, remains readable without horizontal overflow. The measured document widths were 375 px at mobile and 753 px within the 768 px tablet viewport.
- Authenticated teacher captures at 375 px and 768 px confirmed that the selected-class roster, CSV import panel, and term-report controls reflow without horizontal overflow. The mobile layout stacks the importer and date controls into touch-sized actions at a 375 px document width; the tablet layout retains a readable balanced control row within a 753 px document width at a 768 px viewport.
- Stateful teacher captures re-applied Ms Kelly’s class selection and the July 1–August 31 range before each screenshot. At both 375 px and 768 px, the captures recorded selected class `1`, both date values, only `Jul 2026` and `Aug 2026` chart labels, and enabled import controls; scroll widths remained 375 px and 753 px respectively.
- Before changing reminder state, authenticated parent captures showed the unread-state controls at 375 px and 768 px. The mobile capture visibly stacked the header badge (`2`), “2 new” status, “Mark all read” button, and individual reminder actions in a 375 px document; the tablet capture kept the same controls readable in the reminder header at 753 px within a 768 px viewport.
- Final validation completed with `pnpm check` and 14 passing test files / 40 passing tests. The test suite includes CSV parsing, inclusive term filtering, range-labelled export filenames, database-backed bulk roster creation with duplicate-row feedback, and parent bulk reminder-read coverage. Recent browser-console logs contained no errors; the only matching development-log warnings are historical, pre-fix Express cookie deprecation entries from September 2.
- The authorised teacher selected Ms Kelly’s Reading Class, loaded a standard MIS export using `Pupil Name`, `Year Group`, and `Admission Number` headings, and saw the mapping preview automatically choose `Pupil Name` for learner name and `Year Group` for the optional level field. Importing the mapped file created Maya Singh while reporting the existing Amina Roe row as already present in the selected class, confirming both mapping and existing-roster duplicate protection.
- The teacher saved “Summer progress check” for 2026-07-01 through 2026-08-31. The new saved-term selector restored that range and reduced the class trend view to Jul 2026 and Aug 2026, confirming that the reusable preset drives the same chart/report range.
- The authorised parent selected Amina Roe in reminder history and applied the inclusive 2026-09-04 date range. The Family reminder centre displayed only the matching September 4 completion notice, while the existing unread badge semantics remained family-wide.
- Authenticated parent captures at 375 px and 768 px confirmed the refined Family Reading Space remains readable without horizontal overflow. The mobile view stacks child/date filters before the history; the tablet view preserves a compact inline filter row. Measured document widths were 375 px and 753 px respectively.
- Stateful teacher captures re-applied class `1` and the 2026-07-01 to 2026-08-31 range. The refined Class manager, MIS-import onboarding, learner controls, saved-term controls, and filtered chart stacked cleanly at 375 px and remained balanced at 768 px; document widths were 375 px and 753 px, with only Jul 2026 and Aug 2026 shown in the chart at both sizes.
- Final validation completed with `pnpm check` and 14 passing test files / 41 passing tests. The integration suite now covers saved named term presets, mapped MIS imports, existing-roster duplicate protection, and parent reminder child/date filtering. After a clean development-server restart, no current browser-console errors were present; the log retains only a historical module-reload syntax error that was corrected before the final successful suite and live browser validation.
- The teacher-approved Irish variation export was validated for Ms Kelly’s selected class. The protected CSV included the class name, expected word, recognised form, and reviewed UTC timestamp for the approved `three` / `tree` pair.
- The dedicated `Pending speech matches` teacher tab was opened in an authorised session and showed the correct class-wide zero state when no provisional matches were pending; database integration coverage exercises the matching teacher-confirmation path.
- The child reading canvas showed `Ready to listen` before recording. With microphone access denied in the browser environment, it switched to `Live listening unavailable` with clear guided-practice fallback language, rather than claiming active recognition.
- Authenticated teacher captures at 375 px and 768 px confirmed that approved-variation export, educator controls, and the pending-review tab remain readable without horizontal overflow. Document widths were 375 px at mobile and 753 px at tablet.
- Final validation passed with `pnpm check` and 17 test files / 50 tests. Recent browser-console logs contained no errors. Development logs retain only historic September 4 startup errors that were resolved before this final successful run.

### Irish English variation support

- The supplied accent-aware phonics architecture was reviewed and its multi-pronunciation principle was adapted as a deliberately small, opt-in, teacher-review-led transcript variation list rather than a dialect classifier. Ms Kelly saved **Irish English support · teacher review** for Amina Roe; the persisted plan remained visible after fresh teacher and child sign-ins, while the child reading canvas retained the selected Guided Practice mode. The live browser uses `en-IE` speech-recognition locale, and server transcription is prompted to preserve, rather than normalise, Irish English regional speech.
- The completed suite passed with **15 test files / 46 tests**. It covers opt-in reviewed variants, unchanged standard comparison, live word-state behaviour, teacher-review event actions, and database persistence of the learner support profile. A 375 px authenticated teacher capture measured a 375 px document width with no horizontal overflow; the new reading-voice selector, safeguard text, and save controls reflowed into a readable single-column layout.
- In an authorised teacher session, Ms Kelly selected **Irish English support · teacher review** as the class reading-voice default and saved it. The dashboard confirmed that this applies only to learners added later and preserves existing individual plans. The teacher then approved the reviewed pair **three → tree**, which appeared immediately in the class-specific list with a removal control.
- The new approval workflow’s automated coverage passed: an added learner inherits the selected class default, an authorised teacher can approve a variant, a provisional reading match is persisted, and the teacher confirmation action records it as confirmed and removes it from the pending review list. Unit coverage also confirms that educator approval takes precedence over a matching built-in entry while remaining unavailable under Standard English comparison.
- Recognition responsiveness was improved by batching interim browser transcript rendering, safely restarting supported continuous browser recognition, using speech-friendly microphone processing constraints, increasing recorder chunk duration to one second, and starting recording storage plus server transcription concurrently. The UI now explains that live word hints depend on browser recognition, while detailed saved-recording analysis completes after the child finishes; no fixed latency guarantee is made.
- Authenticated teacher captures at 375 px and 768 px show the class default selector, approved-variant editor, saved `three → tree` pair, bulk-import guide, learner-plan controls, and review panel with responsive readable reflow. Measured document widths were 375 px at mobile and 753 px within the 768 px tablet viewport, so no horizontal overflow was introduced.
- Final validation completed with TypeScript checking cleanly and **16 test files / 48 tests** passing. The current browser console is empty. Earlier development-log error entries are historical from pre-fix hot-reload/database schema transitions and did not recur in the current teacher validation session.

### Navigation, connection, and class review enhancements

- In the authorised parent dashboard, both **Reading Library — Child sign-in** and **Teacher Dashboard — Teacher sign-in** are visible alongside the parent-only dashboard link. Selecting Reading Library signs out the parent session and opens the Child demo password card, confirming a secure workspace handoff rather than cross-role data exposure.
- The teacher selected Ms Kelly’s Reading Class, opened the dedicated Pending Speech Matches tab, and saw learner, From/To session-date controls, a filter action, and a class-scoped Print class review action. The protected PDF download completed as a valid A4, one-page PDF titled “Ms Kelly’s Reading Class Class Variation Review.”
- In the child reading canvas, the new connection tile showed “Connection looks good” separately from “Ready to listen.” When the browser denied microphone access, the recognition tile changed to “Live listening unavailable” and offered guided practice; the connection tile did not misrepresent this as a reading result.
- Targeted coverage for the connection classifier, printable PDF, and filtered teacher review passed. The teacher-review database integration asserts class, learner, and inclusive saved-session-date filtering along with report data aggregation.
- State-preserved authenticated captures confirmed the child reading canvas at 375 px keeps the ready-to-listen and local connection tiles clearly separated, with a 375 px document width and no horizontal overflow. The selected-class Pending Speech Matches tab was also inspected at 375 px and 768 px: learner/date filters and Print class review remain visible and touch-accessible, with document widths of 375 px and 753 px respectively.
- Final validation completed with TypeScript checking cleanly and **19 test files / 52 tests** passing. The current browser console contains no errors. Development logs retain one historical September 4 module-export error from an earlier hot reload; it did not recur in the current source state or final validation suite.

### Guided reading, weekly goals, and family support

- The child flow was checked from the Reading Library. Opening the teacher-assigned Lantern story displayed the accessible **Vocabulary & Sound Warm-Up** before the reading canvas, with three preview words, local “Hear word” actions, a sound-focus prompt, and a clear Start guided reading action. The canvas then showed the Guided Practice badge, real-time word-state treatment, the recognition state, and separate connection-quality hint. When the browser denied microphone access, the state changed honestly to “Live listening unavailable” and retained a guided-practice completion path rather than claiming to be listening.
- The authorised teacher dashboard saved Amina’s current-week 25-minute / 3-session goal and refreshed its 3/25 minutes and 3/3 sessions progress summary. Parent validation confirmed a last-story-aware five-minute co-reading modal and the corrected authorised best-moment playback control, which selected the latest stored recording even though the newest session had no audio.
- Authenticated parent captures at 375 px and 768 px confirmed the monthly trend, fluency trajectory, practice card, reminder centre, parent summary, and best-moment controls remain visible and readable. The 375 px view stacked each panel without overflow; the 768 px view retained balanced two-column metric cards. Their measured document widths were 375 px and 753 px respectively.
- Authenticated teacher captures at 375 px and 768 px confirmed the Class manager, categorised Speech Review Panel, learner reading-context cards, weekly-goal controls, class overview, and saved-session panel all remain available in responsive layouts. The mobile dashboard uses a single-column reading order at a 375 px document width; tablet retains a readable grouped overview within a 753 px document width.

### Immersive Kids Mode and move-on practice

- In the authenticated child demo, opening the assigned Lantern story displayed the Vocabulary & Sound Warm-Up before launch. Starting it then rendered the simplified Kids Mode canvas with no top bar, side navigation, mission card, mode picker, technical status tiles, coach/debug panels, transcript preview, or support-card footer. The live page contains only Back to Reading Library, story title, Page 1 of 3, progress track, large centred passage text, Hear page, Tap to Read, and Finish story.
- Unit coverage confirms a three-attempt moved-on word remains marked incorrect while the live transcript aligns subsequent recognised words correctly. The post-reading report includes the skipped word in the child’s repeat/listen activities and retains authorised access to the saved recording when one is available.
- A state-preserved 375 px child capture measured a 375 px document width with no horizontal overflow. It showed only the back link, story title, page label/progress track, high-contrast large text, and compact floating Hear page, Tap to Read, and Finish story controls. Any fixed Preview Mode strip shown at the capture bottom is environment chrome, not part of Reader Leader’s page.
- Final validation completed with TypeScript checking cleanly and **19 test files / 54 tests** passing. The additional word-state coverage verifies the move-on alignment safeguard; the Kids Mode browser canvas remained free of child-facing teacher/debug and technical system controls.

### Child-flow repair and tabbed Teacher Workspace

- The child warm-up, immersive reader, and empty-session safeguards were exercised in an authorised Amina session. Opening *The Lantern in the Garden* displayed the warm-up before the simplified reader. With microphone permission unavailable in the validation browser, the canvas accurately stayed on the current word and showed the guided-practice fallback; selecting **Finish story** without transcript words or attempts kept the child on the page and displayed “Start reading before finishing,” rather than producing a congratulatory report.
- Model-audio controls call the browser’s native text-to-speech API, so they are not inert text controls. The headless validation browser reported that it could not start audio output and the application showed a clear sound/device message rather than silently doing nothing. Actual speech playback remains device/browser dependent and needs a browser voice with sound enabled. Persisted recording buttons now use a rendered authorised audio element and show only after the saved session has an audio key; a successful captured recording is retained if detailed transcription falls back to the live transcript.
- The teacher workspace was checked with all four requested tabs: **Dashboard & Speech Review**, **Class & Roster**, **Materials Studio & Quiz Generator**, and **Progress & Analytics**. The Quick Start guide displayed its four steps; speech review, roster settings, Voice & Accent Rules, MIS import guide, weekly goals, Materials Studio metadata/source fields, and term-filtered exports remained available through their relevant tabs. Materials Studio accepts dropped or selected PDF, DOCX, and TXT sources and persists the optional lesson summary.
- Final automated validation passed with **20 test files / 56 tests**. Historical server/browser errors predate the current source state; the final TypeScript check completed successfully.

### Keep-reading continuation repair

- The child reader now keeps a persistent transcript base whenever browser recognition pauses or restarts around model audio. If a child misses a word then continues with the following expected word, the missed word is retained as incorrect for post-reading practice, while subsequent words match from that next position rather than from the start of the story.
- The fresh child browser session confirmed the child stays in the active *Lantern* reader with only the core controls. The validation browser denied microphone access, so natural continuation was verified with deterministic live-state and transcript-context tests. Final validation passed with TypeScript checking cleanly and **21 test files / 59 tests** passing.

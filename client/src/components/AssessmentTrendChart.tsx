import { Line, LineChart, CartesianGrid, Legend, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";

export type AssessmentTrendPoint = { month: string; label: string; storyMatch: number; wcpm: number; sessions: number };

export function AssessmentTrendChart({ title, points, compact = false }: { title: string; points: AssessmentTrendPoint[]; compact?: boolean }) {
  if (!points.length) return <section className={`trend-card ${compact ? "compact" : ""}`}><div className="card-title-row"><h2>{title}</h2><span>Monthly Assessment</span></div><div className="trend-empty">Complete a monthly assessment to begin this shared growth view.</div></section>;
  return <section className={`trend-card ${compact ? "compact" : ""}`}><div className="card-title-row"><h2>{title}</h2><span>Monthly Assessment</span></div><p className="trend-caption">Story match and WCPM are shown together as practice signals over time.</p><div className="trend-chart"><ResponsiveContainer width="100%" height={compact ? 210 : 270}><LineChart data={points} margin={{ top: 10, right: 16, left: -18, bottom: 0 }}><CartesianGrid stroke="#e8edf5" strokeDasharray="4 5" /><XAxis dataKey="label" tick={{ fill: "#425172", fontSize: 12 }} axisLine={false} tickLine={false} /><YAxis domain={[0, 160]} tick={{ fill: "#425172", fontSize: 12 }} axisLine={false} tickLine={false} /><Tooltip contentStyle={{ borderRadius: 14, border: "1px solid #e3e8f1", boxShadow: "0 8px 24px rgba(28,46,87,.12)" }} /><Legend iconType="circle" /><Line type="monotone" dataKey="storyMatch" name="Story match %" stroke="#2563eb" strokeWidth={3} dot={{ r: 4, fill: "#2563eb" }} activeDot={{ r: 6 }} /><Line type="monotone" dataKey="wcpm" name="WCPM" stroke="#e56b4f" strokeWidth={3} dot={{ r: 4, fill: "#e56b4f" }} activeDot={{ r: 6 }} /></LineChart></ResponsiveContainer></div></section>;
}
